## Om de node modules te installeren:
run dit via de terminal in de project folder (als node nog niet geinstalleerd is: [https://nodejs.org](https://nodejs.org/en/download/prebuilt-installer)).
```
npm install
```